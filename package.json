{
	"private": true,
	"author": "sragss",
	"workspaces": [
		"zkdrops-contracts",
		"zkdrops-lib",
		"client-ex"
	],
	"scripts": {
		"compile": "cd zkdrops-contracts && npx hardhat compile",
		"start-backend": "cd contracts && npx hardhat node",
		"deploy": "cd contracts && npx hardhat run --network localhost ./scripts/deploy.ts",
		"start-client": "cd client-ex && npm run dev"
	}
}
